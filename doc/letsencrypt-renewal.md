Shutdown the server

Start the server in debug mode

    $ ./server -dp 80

Run the certificate renewer bot

    $ certbot renew

Shutdown the server

Start the server in production mode

    $ ./server -p 80

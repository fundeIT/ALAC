Connect to the server through SSH:

    $ ssh root@alac.funde.org

Start the tmux utility:

    $ tmux

Go to the working directory and run the application

    $ cd ALAC
    $ python tmpsrv.py

Deattach the terminal using the keyboard sequence: `Ctrl+b d`

Close the SSH session:

    $ exit
